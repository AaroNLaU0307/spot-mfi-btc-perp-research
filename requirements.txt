# Pinned to the versions this research was run and audited against (Python 3.13).
# ccxt/requests are only needed to regenerate data_cache/ from public endpoints (run_00_data.py,
# run_A1_eda.py's funding step) — every other script only reads the cached parquet files and has
# no network dependency.
ccxt==4.5.63
pandas==3.0.3
numpy==2.5.0
scipy==1.18.0
statsmodels==0.14.6
matplotlib==3.11.0
pyarrow==24.0.0
pytest==9.1.1
requests==2.34.2
